#include <import>

struct something {
    int pre_pre_processor;
};

// we want to allow for this syntax with A-type

int main(int n_args, char* v_args[]) {
    A_start();
     string a = string("Audrey");
    //await(f);
    return 0;
}
