#include <A>

int main(int n_args, char* v_args[]) {
    printf("doesnt work?\n");
    char* test1 = (char*)calloc(1, 1);
    AF pool = A_pool(1024);

    string audrey;
    audrey = new(string, chars, "Audrey");
    
    
    A_free(pool);
    return 0;
}
